[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "hft_engine"
version = "0.1.0"
description = "O(1) Limit Order Book and DES loop for HFT backtesting"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
